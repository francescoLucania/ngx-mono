import { GenerateInputsDirective } from './generate-inputs.directive';

describe('GenerateInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateInputsDirective();
    expect(directive).toBeTruthy();
  });
});

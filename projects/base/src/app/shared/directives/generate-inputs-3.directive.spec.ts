import { GenerateInputs3Directive } from './generate-inputs-3.directive';

describe('GenerateInputs3Directive', () => {
  it('should create an instance', () => {
    const directive = new GenerateInputs3Directive();
    expect(directive).toBeTruthy();
  });
});
